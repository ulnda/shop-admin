const ERROR = {
  EMPTY: 'Required',
  INVALID_FORMAT: 'Invalid format',
};

const NAME = {
  LOGIN: 'login',
};

const REGEXP = {
  EMAIL: /^[A-Z0-9._%+-]+@[A-Z0-9.-]+\.[A-Z]{2,4}$/i,
};

export default { NAME, ERROR, REGEXP };